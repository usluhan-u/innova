import { extendTheme } from '@chakra-ui/react';

export const theme = extendTheme({
  colors: {
    text: {
      primary: '#002946',
      secondary: '#7893A7',
      blue: '#005A9B',
      light: '#FFFFFF'
    },
    button: {
      primary: '#005A9B'
    }
  }
});
